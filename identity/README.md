# identity

**Language:** Elixir / Ash

**Purpose:** Technicians, dispatchers, auth.

Owns Postgres schema `identity`. Skeleton only (Stage 0) — no `mix.exs` or
`moon.yml` yet; those land in Stage 1 (Moon workspace) and Stage 3
(resources, schemas, roles, per-component Repos).
