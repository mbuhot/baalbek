# moon
.moon/cache
.moon/docker

# Elixir (poncho-style: one set of patterns here covers every Mix project
# added under the repo root, per-project mix.exs directories included —
# no per-app .gitignore duplication needed, see Stage 2 report).
_build/
deps/
*.ez
erl_crash.dump
.elixir_ls/

# Rust (pricing crate + pricing_native's native/pricingnative Rustler NIF
# crate, Stage 4). Build output for both actually lands under
# $HOME/.cache/baalbek-cargo-target/* (see pricing/moon.yml and
# pricing_native/moon.yml — a virtiofs workaround), but a plain `cargo
# test`/`cargo build` run without that env var falls back to an in-tree
# target/, so it's still ignored defensively. Cargo.lock is NOT ignored —
# committed for the same hermeticity reason core's mix.lock is committed.
target/

# Rustler copies the compiled NIF shared library here at build time
# (pricing_native/priv/native/pricingnative.so); it's a build artefact of
# native/pricingnative's source, not source itself. Unanchored (**/) since
# a mid-pattern slash otherwise anchors the match to the repo root, which
# would only match a nonexistent top-level priv/native/ and silently let
# the real nested pricing_native/priv/native/ path get committed.
**/priv/native/

# Every project's Moon-owned build tree — the artefact its `build`/`package`
# task declares as `outputs` (spec/decisions/adr-0006-task-deps-on-output-declaring-tasks.md).
# Anchored to one directory below the root so it never matches a nested
# build directory a native toolchain owns, such as mobile/android/app/build.
# The Elixir apps compile here rather than into `_build/` so a Moon cache
# restore can never write over the tree Mix and ElixirLS share.
/*/build/

# `server`'s generated OpenAPI spec (Stage 6, `server:openapi` Moon task).
# Reproducible from the Ash/AshJsonApi resource declarations it's
# introspected from, same rationale as pricing_native's gitignored
# priv/native/*.so — the source of truth is the DSL, not this artefact.
server/priv/static/

# Assembled releases (Stage 6c, `server:release`). Kept out of every
# project's own directory so no `project://` input walks a 101 MB tree
# while `mix release --overwrite` is rewriting it; dot-prefixed so
# .moon/workspace.yml's `globs: ["*"]` doesn't turn it into a project.
.artifacts/

# TypeScript (Stage 6): pnpm workspace (web, core-api-client). pnpm-lock.yaml
# is NOT ignored, same hermeticity reason mix.lock/Cargo.lock aren't.
node_modules/
.pnpm-store/
dist/
*.tsbuildinfo

# Playwright's HTML report, written by the CI reporter (Stage 9). Traces and
# the browser binaries both live under ~/.cache, outside the repo — see
# e2e/playwright.config.ts's outputDir.
e2e/playwright-report/

# core-api-client's one generated artifact (moon.yml's `build` task,
# openapi-typescript from server's OpenAPI spec) — same rationale as
# server/priv/static/ above, the source of truth is server's Ash/AshJsonApi
# resources, not this file.
core-api-client/src/generated/

# The explorer's model (Stage 10, `explorer:generate`). Derived from the moon
# graph, git history, and capabilities.yml; committing it would let the site
# drift from the workspace it describes, which is the one thing seed.md §9
# says it must not do.
explorer/src/generated/
