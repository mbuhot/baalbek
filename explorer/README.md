# explorer

**Language:** TypeScript

**Purpose:** Static site from project-graph JSON + git stats + `capabilities.yml`.

Self-built interactive architecture explorer, regenerated in CI on every
build so it cannot drift from the real cross-language dependency graph.
Skeleton only (Stage 0) — no `package.json` or `moon.yml` yet; those land in
Stage 1 and Stage 10.
