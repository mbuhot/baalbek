# Ash resources for invoices raised from completed jobs.
#
# Schema `billing` and role `billing`. `deps`, `build` and `test` come from
# ../.moon/tasks/elixir.yml, and `bootstrap` from
# ../.moon/tasks/elixir-repo.yml.
language: elixir
layer: library

# Capabilities this component serves, in explorer/capabilities.yml's vocabulary.
tags:
  - "capability/invoicing"
