# billing

**Language:** Elixir / Ash

**Purpose:** Invoices raised from completed jobs.

Owns Postgres schema `billing`. Skeleton only (Stage 0) — no `mix.exs` or
`moon.yml` yet; those land in Stage 1 (Moon workspace) and Stage 3
(resources, schemas, roles, per-component Repos).
