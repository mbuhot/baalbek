# core

**Language:** Elixir / Ash

**Purpose:** Jobs, customers, sites, work orders. Publishes OpenAPI.

Owns Postgres schema `core`. Skeleton only (Stage 0) — no `mix.exs` or
`moon.yml` yet; those land in Stage 1 (Moon workspace) and Stage 2
(Ash resources, schema + role, `boundary`, ExUnit).
