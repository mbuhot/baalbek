# server

**Language:** Elixir / Phoenix

**Purpose:** Release assembly; HTTP surface; depends on all domain apps.

Publishes the OpenAPI spec generated from Ash (consumed at build time by
`core-api-client`). Skeleton only (Stage 0) — no `mix.exs` or `moon.yml` yet;
those land in Stage 1 and Stage 6.
