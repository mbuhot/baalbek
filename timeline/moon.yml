# Stage 1 stub — see PLAN.md Component inventory.
# Gleam. Schema `timeline`: event-sourced technician shift / travel /
# on-site events; availability projections. No Gleam toolchain plugin
# exists for Moon yet, so this falls back to `system` (fine — no real
# tasks until Stage 5). timeline_facade (Elixir) is its sole caller; that
# cross-language edge is declared as an explicit dependsOn starting Stage 5
# (PLAN.md "Two facades, one pattern").
language: gleam
layer: library
