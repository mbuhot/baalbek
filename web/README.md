# web

**Language:** TypeScript

**Purpose:** PWA: dispatch board + technician view.

Depends on the `core-api-client` package generated at build time from
`core`'s OpenAPI spec. Lives in the pnpm workspace. Skeleton only (Stage 0) —
no `package.json` or `moon.yml` yet; those land in Stage 1 and Stage 6.
