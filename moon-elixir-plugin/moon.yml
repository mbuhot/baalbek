# Stage 1 stub — see PLAN.md Component inventory.
# Rust -> WASM. Mix path-dep inference; replaces check-deps-drift.py in
# Stage 11. Real tasks land in Stage 11.
language: rust
layer: tool

# Carries no business capability; explorer/capabilities.yml records why.
tags:
  - "platform"
